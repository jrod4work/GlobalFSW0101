/* let idealCar = {
    name: 'mustang',
    color: 'red',
    weightInPounds: 3600,
 getmyCar: function() {
    console.log(this.name + " " + this.color + " " + this.weightInPounds);
 }
};

idealCar.getmyCar();
 

const doubleAge = (age , b = 2) => a * b;
doubleAge(2);*/

const addFunction = (param1, param2) => param1 + param2;

addFunction(2, 4);